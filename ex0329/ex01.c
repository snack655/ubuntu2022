#include <stdio.h>
#define MAXLINE 100

// 문자열을 입력 받아 긴 문자열 일 때 출력하는 프로그래밍
void copy(char from[], char to[]);
char line[MAXLINE];
char instr[MAXLINE];

int main() {
	

	// 정상 종료 일 때는 0을 리턴한다.
	return 0;
}

// u 뒤로 가기 ctrl+r 앞으로 가기
// ctrl + z ps 프로세스
// fg 앞으로 다시
// bg
void copy(char from[], char to[]);
}
